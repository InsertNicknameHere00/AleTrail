<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="#2D1F0A">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Badge Icon/Emoji -->
        <TextView
            android:id="@+id/badgeIcon"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:gravity="center"
            android:textSize="40sp"
            android:background="#33FFB300"
            tools:text="🏆"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Badge Name -->
        <TextView
            android:id="@+id/badgeName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Bronze Collector"
            app:layout_constraintTop_toTopOf="@id/badgeIcon"
            app:layout_constraintStart_toEndOf="@id/badgeIcon"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Badge Description -->
        <TextView
            android:id="@+id/badgeDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="#CCFFFFFF"
            android:textSize="14sp"
            android:maxLines="2"
            android:ellipsize="end"
            tools:text="Collect 10 stamps"
            app:layout_constraintTop_toBottomOf="@id/badgeName"
            app:layout_constraintStart_toStartOf="@id/badgeName"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Progress (for locked badges) -->
        <ProgressBar
            android:id="@+id/badgeProgress"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="0dp"
            android:layout_height="6dp"
            android:layout_marginTop="8dp"
            android:progress="50"
            android:max="100"
            android:progressDrawable="@drawable/progress_stamps"
            android:visibility="gone"
            tools:visibility="visible"
            app:layout_constraintTop_toBottomOf="@id/badgeDescription"
            app:layout_constraintStart_toStartOf="@id/badgeName"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/badgeIcon" />

        <!-- Earned Badge Status -->
        <com.google.android.material.chip.Chip
            android:id="@+id/earnedChip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="✓ Earned"
            android:textColor="#FFB300"
            android:textSize="11sp"
            app:chipBackgroundColor="#33FFB300"
            app:chipStrokeColor="#FFB300"
            app:chipStrokeWidth="1dp"
            android:layout_marginTop="12dp"
            android:visibility="gone"
            tools:visibility="visible"
            app:layout_constraintTop_toBottomOf="@id/badgeIcon"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>


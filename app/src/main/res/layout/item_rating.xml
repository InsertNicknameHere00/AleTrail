<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="?attr/aleCardBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <!-- Brewery name -->
        <TextView
            android:id="@+id/ratingBreweryName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="?attr/aleGold"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Crafty Brewery Co." />

        <!-- Beer name -->
        <TextView
            android:id="@+id/ratingBeerName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="?attr/aleTextSecondary"
            android:textSize="13sp"
            tools:text="Beer: IPA Special" />

        <!-- Rating stars -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <android.widget.RatingBar
                android:id="@+id/ratingStars"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:isIndicator="true"
                android:numStars="5"
                android:stepSize="0.5"
                android:scaleX="0.7"
                android:scaleY="0.7"
                android:transformPivotX="0dp" />

            <TextView
                android:id="@+id/ratingValue"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="?attr/aleTextPrimary"
                android:textSize="14sp"
                android:textStyle="bold"
                tools:text="4.5" />
        </LinearLayout>

        <!-- Comment -->
        <TextView
            android:id="@+id/ratingComment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textColor="?attr/aleTextSecondary"
            android:textSize="13sp"
            android:maxLines="3"
            android:ellipsize="end"
            android:visibility="gone"
            tools:text="Great beer, highly recommend!" />

        <!-- Date -->
        <TextView
            android:id="@+id/ratingDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="?attr/aleTextHint"
            android:textSize="11sp"
            tools:text="Mar 5, 2026" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>

